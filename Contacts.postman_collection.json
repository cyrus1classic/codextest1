{
  "info": {
    "name": "Contacts API",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {"name": "Get Contacts", "request": {"method": "GET", "url": "{{baseUrl}}/api/contacts"}},
    {"name": "Create Contact", "request": {"method": "POST", "url": "{{baseUrl}}/api/contacts", "body": {"mode": "raw", "raw": "{}"}}}
  ]
}
